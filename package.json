{
  "name": "blockweavenet",
  "version": "1.0.0",
  "description": "A decentralized network management smart contract system for adding and managing nodes on-chain.",
  "scripts": {
    "test": "npx hardhat test",
    "deploy": "npx hardhat run scripts/deploy.js --network sepolia"
  },
  "dependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "dotenv": "^16.0.3",
    "hardhat": "^2.22.1"
  }
}
